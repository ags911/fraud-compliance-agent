# Archived dashboard prototype

This standalone React/Vite prototype was retained as visual source evidence
from the API snapshot. It is not the active operator console and is not built,
tested, or deployed by the monorepo quality gates.

The active console is [`apps/web`](../../../web). Do not add API contracts,
credentials, runtime dependencies, or new product work here. Preserve it only
as a reference until an explicit archive or deletion decision is made.
